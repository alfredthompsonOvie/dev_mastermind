import MainSpinner from "@/app/_components/ui/main-spinner";

export default function Loading() {
	// You can add any UI inside Loading, including a Skeleton.
	// return <LoadingSkeleton />
	return <MainSpinner />;
}
